spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          url: xxxxxx
          username: xxx
          password: xxx
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录